# Railway.app deployment configuration
# This file configures how Railway deploys your frontend

[[build]]
builder = "DOCKERFILE"
dockerfilePath = "frontend-gomaintenance/Dockerfile"
dockerContext = "frontend-gomaintenance"

[deploy]
startCommand = "node server.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
